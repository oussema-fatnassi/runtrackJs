function quote() {
    console.log(document.getElementById("quote").textContent);              // Show the quote in the console           
    alert(document.getElementById("quote").textContent);                    // Show the quote in an alert
}